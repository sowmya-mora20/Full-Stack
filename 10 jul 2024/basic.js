console.log("hello world");// print like statements will be visible in console(insepct>>console)
//3 methods to define variables-> var(not used nawadays, older way), 
//let(more used these days, modern way), 
//const(used to declare variable, but the ines, whose value are not changeable)
var a=1;
const b=5;
let c=a+b;
console.log(c);